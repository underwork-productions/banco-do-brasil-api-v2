<?php

declare(strict_types=1);

/*
|--------------------------------------------------------------------------
| Test Case
|--------------------------------------------------------------------------
|
| Bind the base test case to every test in the suite. Add project-wide
| expectations or helpers below as the suite grows.
|
*/

// uses(Tests\TestCase::class)->in('Feature');
